# facebook2ghost

Import posts from fanpage into ghost blog

## Requirements
```javascript
npm install fb
```

On linux ENV variable must be set:

```
export FB_SITE=''
export FB_ACCESSTOKEN=''
```
